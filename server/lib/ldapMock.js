/*
 * Mock LDAP server, useful for testing and development
 *
 */

const ldap = require("ldapjs");

ldapServer = ldap.createServer()

// obviously we need to add a bit more to this :D 

module.exports = exports = ldapServer;
